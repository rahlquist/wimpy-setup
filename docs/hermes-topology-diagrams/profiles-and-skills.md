# Hermes Profiles and Skills Inventory

Generated from the `hermes-profiles` checkout. Profiles: **39**. Shared skills: **54**.

> Sanitized inventory: names, descriptions, and link topology only. It does not publish `SOUL.md`, `AGENTS.md`, credentials, memory, or session data.

## Profile → linked skills

### `backend-engineer`

Backend engineer — implements API endpoints (REST/gRPC/GraphQL), service logic, database access patterns, integration code, and service-level observability. Translates architecture designs into working server-side systems.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `backend-engineering` (shared skill: `backend-engineering`)

### `brand-designer`

Brand identity specialist — creates comprehensive brand documentation covering strategy, visual identity (logo, color, typography, imagery), voice/tone, and usage guidelines.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `brand-designer` (shared skill: `brand-designer`)

### `ceo`

>-

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `executive-methodology` (shared skill: `executive-methodology`)
- `mermaid-diagrams` (shared skill: `mermaid-diagrams`)
- `strategy-frameworks` (shared skill: `strategy-frameworks`)

### `cfo`

>-

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `executive-methodology` (shared skill: `executive-methodology`)
- `financial-modeling` (shared skill: `financial-modeling`)

### `chief-of-staff`

>-

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `chief-of-staff-methodology` (shared skill: `chief-of-staff-methodology`)
- `executive-methodology` (shared skill: `executive-methodology`)
- `operational-design` (shared skill: `operational-design`)
- `orchestration-methodology` (shared skill: `orchestration-methodology`)
- `org-design` (shared skill: `org-design`)

### `chro`

CHRO — owns organizational design, talent strategy, and culture architecture. Evaluates whether the right people exist for the work, identifies skill gaps, designs compensation and incentive structures, manages the hiring pipeline, and assesses organizational health. Optimizes for organizational capability, talent density, and cultural coherence.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `executive-methodology` (shared skill: `executive-methodology`)
- `org-design` (shared skill: `org-design`)

### `clo`

CLO/General Counsel — owns legal strategy, regulatory risk, and IP protection. Flags regulatory exposure in proposals, evaluates open-source license compatibility, assesses contract and liability risk, designs IP protection strategy, and reviews for compliance with relevant frameworks. Optimizes for legal risk minimization, regulatory compliance, and IP defensibility.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `executive-methodology` (shared skill: `executive-methodology`)
- `legal-strategy` (shared skill: `legal-strategy`)

### `cmo`

CMO — owns go-to-market strategy, brand, and customer acquisition. Defines positioning and messaging, designs acquisition strategy, builds brand architecture, models growth channels, and evaluates market entry timing. Optimizes for market presence, customer acquisition efficiency, and brand coherence.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `executive-methodology` (shared skill: `executive-methodology`)
- `go-to-market` (shared skill: `go-to-market`)

### `coo`

>-

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `executive-methodology` (shared skill: `executive-methodology`)
- `operational-design` (shared skill: `operational-design`)

### `copy-editor`

Line-level editor — grammar, punctuation, consistency, style guide enforcement, and proofreading. Sits after editor (structure) and before verifier (publication gate).

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `copy-editor-methodology` (shared skill: `copy-editor-methodology`)

### `cpo`

CPO — owns product strategy and market fit. Defines what to build and why, prioritizes features against strategic goals, evaluates competitive landscape, and translates user needs into product requirements. Complements the product-manager profile at the strategic level. Optimizes for user value delivery, market differentiation, and product-market fit.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `executive-methodology` (shared skill: `executive-methodology`)
- `mermaid-diagrams` (shared skill: `mermaid-diagrams`)
- `product-methodology` (shared skill: `product-methodology`)
- `product-strategy` (shared skill: `product-strategy`)

### `cto`

>-

- `architecture` (shared skill: `architecture`)
- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `executive-methodology` (shared skill: `executive-methodology`)
- `mermaid-diagrams` (shared skill: `mermaid-diagrams`)
- `technology-radar` (shared skill: `technology-radar`)

### `curator`

Knowledge curator — manages notes, creates and cross-links atomic content, ingests sources, and maintains knowledge graph connectivity.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `curation-methodology` (shared skill: `curation-methodology`)

### `data-architect`

Data architect — data modeling, pipeline architecture, storage strategy, schema design, governance, and platform evaluation. Produces artifact-pyramid output with tradeoff analysis.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `data-architect` (shared skill: `data-architect`)

### `data-engineer`

Data engineer — database operations (relational, vector, graph, time-series), ETL/ELT pipeline design with dbt, analytical SQL patterns, schema migrations, data quality monitoring, and backup/recovery. Keeps data infrastructure healthy, performant, and evolvable across multiple storage paradigms.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `data-engineering` (shared skill: `data-engineering`)

### `data-scientist`

PhD-level data scientist — statistical modeling, causal inference, experimental design, machine learning, and reproducible research. Runs assumption diagnostics, power analysis, and produces rigorous analysis reports.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `data-scientist` (shared skill: `data-scientist`)

### `debugger`

Systematic debugger — finds root causes before fixing. Expert in error investigation, reproduction, and test-driven debugging.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `debugging-methodology` (shared skill: `debugging-methodology`)
- `systematic-debugging` (shared skill: `systematic-debugging`)

### `editor`

Shapes drafts into publishable work — structural editing, argument coherence, engagement quality, voice consistency, factual accuracy. Sits between writer (creates) and copy-editor (polishes).

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `editor-methodology` (shared skill: `editor-methodology`)
- `editor-review-methodology` (shared skill: `editor-review-methodology`)

### `frontend-engineer`

Frontend engineer — implements UI components, state management, API integration, responsive layouts, and client-side performance. Translates UX designs into working, accessible, performant user interfaces.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `frontend-engineering` (shared skill: `frontend-engineering`)

### `implementation-planner`

Implementation planner — maps work breakdown structures, identifies dependency chains, analyzes critical paths, sequences build phases, and produces milestone plans with risk buffers and rollback strategies.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `implementation-planning` (shared skill: `implementation-planning`)

### `kanban-strategist`

Kanban flow strategist — diagnoses flow problems, designs board configurations, calibrates WIP limits, and sets up multi-portfolio operating models. Hermes-specific (uses Hermes kanban).

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `kanban-guru` (shared skill: `kanban-guru`)

### `ml-engineer`

ML engineer — model training, fine-tuning (LoRA/QLoRA), evaluation, quantization, and inference serving methodology. Bridges model research and production deployment through reproducible pipelines and rigorous evaluation.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `ml-engineering` (shared skill: `ml-engineering`)

### `orchestrator`

Orchestrator — decomposes complex questions into specialist workflows, routes work to the right profile in the right sequence, and synthesizes their outputs into coherent results. Owns the conversation structure, not the execution.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `orchestration-methodology` (shared skill: `orchestration-methodology`)

### `oss-contributor`

Open source contributor — follows CONTRIBUTING.md-first protocol, handles cross-fork PRs, manages the release lifecycle (versioning, CHANGELOG, tagging, publishing), and follows project norms for bug reports, feature requests, and code contributions.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `opensource-contributions` (shared skill: `opensource-contributions`)

### `platform-engineer`

Platform engineer — builds and operates the internal developer platform. Owns CI/CD pipelines, infrastructure as code (Terraform/OpenTofu/Pulumi/Ansible), container orchestration (K8s, Helm, Docker Compose), service networking (Traefik, Tailscale), observability infrastructure, release engineering (container images, charts, internal packages), and the deployment pipeline that makes services reachable. Produces architecture assessments, pipeline designs, infrastructure plans, and deployment strategies as artifact pyramids.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `docker-management` (shared skill: `docker-management`)
- `implementation-planning` (shared skill: `implementation-planning`)
- `mermaid-diagrams` (shared skill: `mermaid-diagrams`)
- `platform-engineering` (shared skill: `platform-engineering`)
- `tailscale` (shared skill: `tailscale`)
- `traefik` (shared skill: `traefik`)

### `product-manager`

Writes specs, manages roadmaps, and aligns teams — translates customer needs into prioritized deliverables. Skilled at stakeholder communication, prioritization frameworks (RICE, MoSCoW, Opportunity Solution Trees), and saying no.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `product-methodology` (shared skill: `product-methodology`)

### `qa-engineer`

QA engineer — test strategy, test automation, regression testing, and quality gates. Designs testing approaches that make confident shipping routine.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `qa-methodology` (shared skill: `qa-methodology`)

### `researcher`

Deep-dive research specialist — scans sources, gathers evidence, triangulates findings, and produces structured briefs and synthesis reports.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `research-methodology` (shared skill: `research-methodology`)
- `researcher-workflow` (shared skill: `researcher-workflow`)

### `reviewer`

Code and architecture reviewer — reviews PRs, runs quality gates, audits for regressions, and verifies against standards.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `review-methodology` (shared skill: `review-methodology`)

### `security-engineer`

Security engineer — threat modeling, vulnerability assessment, security architecture review, and penetration testing methodology. Evaluates systems for weaknesses across code, infrastructure, dependencies, and operations. Grounded in OWASP, CWE, and STRIDE frameworks.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `security-audit-methodology` (shared skill: `security-audit-methodology`)

### `seo-specialist`

SEO specialist — search engine optimization strategy, content optimization, metadata management, and search performance analysis. Covers technical SEO, on-page SEO, schema markup, and content strategy for search.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `seo-audit` (shared skill: `seo-audit`)
- `seo-content-optimization` (shared skill: `seo-content-optimization`)

### `site-reliability-engineer`

Site reliability engineer — designs reliability into production systems through SLO/SLI frameworks, error budget governance, incident command, observability engineering, and operational excellence. Produces reliability posture assessments, postmortems, runbooks, and automation plans as artifact pyramids.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `implementation-planning` (shared skill: `implementation-planning`)
- `mermaid-diagrams` (shared skill: `mermaid-diagrams`)
- `site-reliability-engineering` (shared skill: `site-reliability-engineering`)
- `systematic-debugging` (shared skill: `systematic-debugging`)

### `spec-driven-development`

Spec-Driven Development specialist — authors formal specifications, decomposes them into executable task plans, generates CLAUDE.md directives for implementation agents, verifies output against acceptance criteria, and enforces phase gates between every SDD pipeline stage. All output is a single path to the artifact-pyramid 00-index.md.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `sdd-authoring` (shared skill: `sdd-authoring`)
- `sdd-review` (shared skill: `sdd-review`)
- `sdd-verification` (shared skill: `sdd-verification`)
- `sdd-work-decomposition` (shared skill: `sdd-work-decomposition`)

### `technical-architect`

Systems architect — designs service boundaries, API contracts, deployment topology, and operational infrastructure. Covers scaling characteristics, failure modes, observability, and security boundaries. Runs discovery sessions and produces architecture decision records. Outputs architecture documentation as artifact pyramids.

- `architecture` (shared skill: `architecture`)
- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `mermaid-diagrams` (shared skill: `mermaid-diagrams`)
- `software-architecture-analysis` (shared skill: `software-architecture-analysis`)

### `technical-writer`

Technical writer — API documentation, README authorship, CLI help text, agent-facing docs (AGENTS.md), developer guides, and reference documentation. Covers the documentation craft between the writer (editorial) and the code.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `technical-documentation` (shared skill: `technical-documentation`)

### `ux-designer`

UX designer — maps user journeys, designs interaction flows, applies accessibility standards, and ensures product decisions account for human experience.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `ux-methodology` (shared skill: `ux-methodology`)

### `verifier`

Pass/fail gatekeeper — verifies work against completion criteria, assesses evidence quality, and holds or passes the verification gate. Differs from reviewer: verifier checks against criteria and makes binary decisions, not quality suggestions.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `verification-methodology` (shared skill: `verification-methodology`)

### `wonderer`

Lateral exploration specialist — explores the periphery of a seed topic, finds adjacent domains and overlooked angles. Expands possibility space before anyone converges.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `wonderer-methodology` (shared skill: `wonderer-methodology`)

### `writer`

Drafts articles, blog posts, documentation, and long-form content. Owns structure, voice, and narrative flow.

- `artifact-pyramids` (shared skill: `artifact-pyramids`)
- `editorial-methodology` (shared skill: `editorial-methodology`)

## Shared skills not linked by any profile

- `contribution-pipeline`

Total unlinked shared skill directories: **1**.

## Shared skill catalog

- `architecture` — Architecture Decision Records — decision capture, rationale documentation, alternatives analysis, and consequence tracking. Maps ADRs to Artifact Pyramid layers (navigation index→L1, active ADRs→L2, superseded ADRs→L3). Use when the technical-architect profile needs to document architectural decisions.
- `artifact-pyramids` — >-
- `backend-engineering` — Backend engineering methodology — API implementation patterns (REST, gRPC, GraphQL), service architecture (clean/hexagonal/layered), database access patterns, integration and middleware design, error handling, and service-level testing. Language and framework agnostic.
- `brand-designer` — >-
- `chief-of-staff-methodology` — >-
- `contribution-pipeline` — Full issue-to-PR lifecycle with 5-phase gated pipeline. Produces contributions maintainers look forward to — project-aware, thoroughly tested, security-gated, well-documented. Skill bundle with sub-skills for security, test quality, documentation, clean-room verification, and vault integration.
- `copy-editor-methodology` — Copy-editing methodology — style guide enforcement, grammar and punctuation, consistency checking, and proofreading protocols.
- `curation-methodology` — Knowledge curation methodology for the knowledge vault — atomic note principles, cross-linking heuristics, split/merge decisions, graph health diagnostics, and the curation cycle. Grounded in Zettelkasten and Molecular Notes philosophy.
- `data-architect` — >-
- `data-engineering` — Data engineering methodology — database operations (vector, relational, graph, time-series), ETL/ELT pipeline design (dbt patterns, incremental loading), SQL analytical patterns, data quality monitoring, schema migration, and storage infrastructure management. Grounded in operational patterns for production data systems.
- `data-scientist` — >-
- `debugging-methodology` — Systematic debugging methodology — root cause analysis, error reproduction, isolation techniques, and verification protocols.
- `docker-management` — Container lifecycle management: Dockerfiles, multi-stage builds, Compose stacks, registries, container networking, and production container patterns.
- `editor-methodology` — Professional editorial methodology — argument coherence, structural editing, engagement quality, voice consistency, and the artifact-pyramid editorial pipeline. Use when the editor profile needs procedural depth beyond the editorial pass.
- `editor-review-methodology` — Editorial review methodology — fact-checking, voice audit, engagement review, and source integrity assessment.
- `editorial-methodology` — Editorial methodology — voice discipline, article structure patterns, source hygiene, and revision protocols for content creation.
- `executive-methodology` — >-
- `financial-modeling` — >-
- `frontend-engineering` — Frontend engineering methodology — component architecture, state management, API integration, responsive layout, client-side performance, and frontend testing patterns. Framework agnostic, focused on web frontend implementation.
- `go-to-market` — >-
- `implementation-planning` — Implementation planning methodology — work breakdown, dependency analysis, critical path identification, risk assessment, and rollback planning.
- `kanban-guru` — >-
- `legal-strategy` — >-
- `mermaid-diagrams` — Platform-agnostic Mermaid.js diagramming — flowcharts, sequence diagrams, C4 (via Structurizr or flowchart workarounds), class/state/ER diagrams, Gantt, and block diagrams. Generates valid Mermaid syntax, renders via mmdc CLI or CDN, and validates before output. Portable — works in CLI, markdown, HTML, PDF, and any agentic context.
- `ml-engineering` — Machine learning engineering methodology — model training, fine-tuning (LoRA/QLoRA), evaluation, quantization, deployment, and MLOps pipeline design. Grounded in practical engineering patterns for production ML systems.
- `opensource-contributions` — >-
- `operational-design` — >-
- `orchestration-methodology` — Orchestration methodology — task decomposition, specialist routing, synthesis patterns, and multi-agent workflow design.
- `org-design` — >-
- `platform-engineering` — Infrastructure as code, CI/CD, container orchestration, service networking — methodology and reference patterns for building and operating internal developer platforms.
- `product-methodology` — >-
- `product-strategy` — >-
- `qa-methodology` — Quality assurance methodology — test strategy design, test automation patterns, regression testing, CI quality gates, test data management, and quality metrics. Grounded in practical patterns for teams that want confident shipping.
- `research-methodology` — Professional research methodology — journalistic investigation, industry analysis, technical verification, and academic-style systematic research. References codify source evaluation, ACH, synthesis, interview handling, signal detection, and reproduction standards.
- `researcher-workflow` — >-
- `review-methodology` — Professional review methodology — code review, security audit, architectural review, and kanban swarm verification. References codify Google's code review standards, OWASP audit patterns, and architectural assessment frameworks.
- `sdd-authoring` — Specification authoring for AI-native SDD — writes formal specifications in structured formats (Gherkin, user stories, acceptance criteria), enforces spec quality gates, and produces artifact-pyramid-compliant SPEC.md outputs. Trigger when specifications or requirements documents need to be authored from scratch or refined.
- `sdd-review` — SDD phase-gate review — enforces the SDD pipeline's gated structure by reviewing specification artifacts before the next phase can proceed. Produces artifact-pyramid-compliant gate decision reports. No phase passes without reviewed, approved artifacts.
- `sdd-verification` — SDD acceptance criteria verification — maps specification acceptance criteria to tests, validates implementation output against spec requirements, and produces artifact-pyramid-compliant VERIFICATION.md reports. Trigger when implementation needs to be verified against its specification.
- `sdd-work-decomposition` — SDD work decomposition — translates formal specifications into dependency-aware task plans with per-task acceptance criteria. Owns the mapping from specification to executable work units, producing artifact-pyramid-compliant TASK-PLAN.md outputs. Trigger when a reviewed specification needs to be broken into implementable tasks.
- `security-audit-methodology` — Security audit and vulnerability assessment methodology — threat modeling, vulnerability classification, defense-in-depth review, dependency analysis, and security architecture evaluation. Grounded in OWASP, CWE, and STRIDE/LINDDUN frameworks.
- `seo-audit` — Full-spectrum SEO + AEO audit skill — technical crawl analysis, on-page content optimization, structured data (JSON-LD/Schema.org) validation, Ghost CMS metadata completion, answer engine optimization (AEO/GEO), content strategy gap analysis, and reporting. Produces artifact-pyramid-compliant output. Use when the seo-specialist profile needs to audit a site, optimize an article for search and AI citation, complete Ghost CMS metadata, or develop a content strategy.
- `seo-content-optimization` — Audit and optimize content sites for search — page title audits, schema injection (Organization, FAQ, LocalBusiness), meta description rewrites, URL hygiene, local landing pages, and measurement. Complements /content-strategy (what to write next) by optimizing what already exists.
- `site-reliability-engineering` — Use when designing, operating, reviewing, or improving production reliability with SLOs, incident command, observability, error budgets, and operational excellence.
- `software-architecture-analysis` — >-
- `strategy-frameworks` — >-
- `systematic-debugging` — >-
- `tailscale` — WireGuard-based overlay networking — mesh VPN, ACL policies, subnet routing, exit nodes, and Headscale self-hosted control server.
- `technical-documentation` — Technical documentation methodology — API reference documentation, README authorship, CLI help text design, agent-facing documentation (AGENTS.md), developer guides, and documentation site information architecture.
- `technology-radar` — >-
- `traefik` — Reverse proxy, load balancer, and ingress controller — dynamic routing, TLS automation, middleware chains, and integration with Docker, Kubernetes, and file-based providers.
- `ux-methodology` — UX design methodology — user research, interaction design, accessibility standards, and usability evaluation.
- `verification-methodology` — Verification methodology — criteria-driven gatekeeping, evidence assessment, and completion verification.
- `wonderer-methodology` — Use when exploring adjacent ideas, overlooked angles, or lateral research paths without prematurely collapsing into a single answer.
